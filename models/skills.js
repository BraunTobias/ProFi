class Skill {
    constructor(id, name, prog, design, social, etc) {
      this.id = id;
      this.name = name;
      this.prog = prog;
      this.design = design;
      this.social = social;
      this.etc = etc;
    }
  }
  
  export default Skill;
  